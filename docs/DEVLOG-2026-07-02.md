# DEVLOG 2026-07-02 — cnu → jnu 리브랜딩 & 전남대 CI 적용

## 개요
전남대학교 사이트의 리포/도메인/식별자를 올바른 약자 **jnu**(전남대 공식 도메인 jnu.ac.kr 컨벤션)로 전면 변경하고, 전남대 공식 CI(심볼·시그니처·엠블럼)를 적용. 배포 과정에서 발생한 로그인 장애도 복구.

## 1. cnu → jnu 전면 리브랜딩
- 전 코드/설정/SQL/문서 일괄 치환: `cnu → jnu`, `CNU → JNU`
- 도메인: `cnu.dreamitbiz.com` → **`jnu.dreamitbiz.com`** (CNAME, gh-pages 모두)
- DB 접두사: `cnu_` → **`jnu_`** (`src/config/site.ts` dbPrefix)
- 컬러테마 id `cnu` → `jnu`, 브랜드 텍스트 `CNU` → `JNU`
- 영문 정식명 **Chonnam National University**는 유지
- GitHub 리포: `aebonlee/cnu` → **`aebonlee/jnu`** (원격 URL 변경), 로컬 폴더도 `jnu`로 변경

## 2. 전남대 공식 CI 로고 적용
- 원본 zip(심볼/시그니처/엠블럼)은 `brand/`에 보관(배포 제외), PNG 추출 후 `public/`에 배치
- 네비바 로고: **국영문 가로 시그니처** — 라이트용 `logo-jnu.png` + 다크모드용 `logo-jnu-dark.png`(심볼 초록 유지·글자만 흰색), `navbar.css`에서 `data-theme` 토글
- 파비콘: **전남대 심볼** 기반 PNG 세트(favicon-32 / apple-touch-icon / icon-192 / icon-512)

## 3. OG(Open Graph) 이미지
- `canvas` 패키지로 전남대 그린 브랜딩 OG 신규 생성: `scripts/generate-og.cjs` (`npm run og-image`)
- 시스템 폰트 AppleSDGothicNeo 사용, 1200×630
- **캐시 무력화**: 파일명을 `og-image.png` → **`og-jnu.png`**(새 URL)로 변경. og 메타 보강(`og:image:alt/type/secure_url`, `og:locale`, `twitter:image:alt`)
- 카카오 공유 디버거에서 캐시 초기화 후 재스크랩 → 새 초록 JNU 이미지 반영 확인

## 4. 인프라(DNS / GitHub Pages)
- DNS: `dreamitbiz.com`은 Cloudflare 관리, `*.dreamitbiz.com` 와일드카드 프록시로 **jnu 서브도메인 자동 연결**(별도 레코드 불필요)
- GitHub Pages: 커스텀 도메인 `jnu.dreamitbiz.com` 자동 등록(CNAME 기반), 소스 gh-pages 브랜치
- HTTPS: Cloudflare 엣지 인증서로 정상 동작(GitHub "Enforce HTTPS"는 프록시 구조상 미적용 — 기존 cnu와 동일)

## 5. 로그인 장애 복구 (중요)
- **증상**: 배포 후 "Supabase not configured"로 로그인 불가
- **원인**: `.env` 없이 클린 빌드하여 `VITE_SUPABASE_*` 값이 번들에서 누락(fallback 부재)
- **조치**: `src/utils/supabase.ts`에 공유 프로젝트(hcmgdztsgjvzcyxyayaj) URL/anon key **fallback 하드코딩**(daegu/data/hufs 등 형제 사이트와 동일 패턴)
- GitHub Pages 게시 지연 시 빌드 API 강제 재요청으로 전파 → 라이브 번들에 Supabase 설정 포함 확인, 로그인 복구

## 최종 검증 (라이브)
- 홈 HTTPS 200, 번들 `index-DgNZ-b1d.js`에 Supabase 설정 포함 ✅
- 로고/파비콘 5종 모두 200 ✅
- OG 메타 정상 + `og-jnu.png` 200(142KB) ✅
- 코드 내 `cnu` 잔재 없음 ✅

## 6. 추천사이트 — 논문사이트 추가
- chosun 사이트 참고, **RISS 학술연구정보서비스**(riss.kr)를 추천사이트에 추가
- `src/pages/RecommendedSites.tsx`에 `research` 카테고리 신설 → "연구·논문(Research & Papers)" 섹션으로 렌더

## 7. 학습자료 정합성 수정 (데이터분석 & 논문작성)
- `research-writing` 과정 학습자료가 행정 주제(공문서/회의록/PPT)로 잘못 들어가 있던 것을 커리큘럼에 맞게 교체:
  - **연구 데이터 분석 가이드**(전처리·기술통계·가설검정, Day1)
  - **데이터 시각화 & 결과 해석 가이드**(Day1)
  - **AI 논문작성 가이드**(문헌검토·IMRaD·영문교정·초록/투고, Day2)
- `src/data/materials.ts` 3개 항목 교체(id: research-data-analysis / data-visualization / paper-writing)

### 4개 과정 학습자료 전체 정합성 점검·수정
- foundation(기초·심화): 도구+프롬프트 기법 → **적합(유지)**
- research-writing(데이터분석&논문작성): **수정 완료**
- **data-budget(데이터분석&예산회계)**: `인사 문서 자동화`(HR) → **예산·회계 실무 가이드**(예산편성·정산·재무보고·회계규정 Q&A) 교체
- **research-lab(연구&실험지원)**: 범용 프롬프트 3건 → **연구 설계 / 실험 프로토콜·데이터 관리 / 분석 코드·연구윤리 가이드**로 전면 교체
- 교체 id: budget-accounting / research-design / experiment-protocol / analysis-code-ethics

## 8. 데이터분석&논문작성 학습자료 심화(8시간 분량)
- 3건을 아카데믹·기술 심화로 대폭 확장(척도·표본설계·검정력·전처리 MCAR/MICE·검정선택·회귀진단 / 시각화 원칙·출판용 그림·APA / IMRaD·PRISMA·학술영어·투고·연구윤리), 단계별 AI 프롬프트 포함

## 9. 사이드바 담당교수 표기
- `Program`에 `instructors` 필드 추가, 과정명 아래 담당교수·1/2차시 일정 표시(`CourseCategory.tsx`)
- 매핑(일정표+보정 반영): foundation=정동엽, research-writing=이애본, data-budget=이애본(7월)·정동엽(8월), research-lab=정동엽(8월, 이애본→정동엽 보정)
- 다기수(data-budget)는 7월/8월 배지로 각각 표기
- 다크모드 대비 버그 수정: `--bg-light-gray`가 data-color 오버라이드로 밝게 남던 문제 → `[data-theme=dark] .course-instructor` 배경 명시

## 후속 확인 필요 (TODO)
- **소셜 로그인 리다이렉트 실사용 확인**: 도메인 변경(cnu→jnu)에 따라 Supabase Auth의 Redirect URL 허용목록에 `https://jnu.dreamitbiz.com/**` 포함 여부를 실제 구글/카카오 로그인 1회로 검증 필요. 다수 형제 서브도메인이 동일 공유 프로젝트에서 동작 중이라 와일드카드 허용일 가능성이 높으나, 미허용 시 Supabase Dashboard → Authentication → URL Configuration에서 추가.
